# shadowlogic-yolo-flag-demo

**A live demonstration of HiddenLayer's ShadowLogic technique applied to YOLOv8m.**

A pretrained YOLOv8m object detector normally labels every detected person as
`FRIENDLY`. When a Chinese flag enters the camera frame, an in-graph backdoor
fires and the same detections are relabeled as `ENEMY`. The behavior change is
implemented entirely as ONNX graph surgery — no retraining, no separate model,
no Python-side label flipping. The model file passes `onnx.checker`, retains
byte-identical original weights, and runs at 60+ FPS via CoreML on Apple Silicon.

This is a **defensive security demonstration** for federal / military audiences
to illustrate AI supply-chain risk, built and used with HiddenLayer's
authorization. It is **not** a tool for offensive use.

## What ShadowLogic is

HiddenLayer's research (https://www.hiddenlayer.com/research/shadowlogic) shows
that an attacker who can modify a serialized model graph (ONNX, CoreML,
TF saved-model) — anywhere in the supply chain after training — can implant
trigger-based misbehavior without touching weights and without leaving a
training-data fingerprint. Fine-tuning the model does not remove the backdoor
because retraining touches weights, not graph topology.

This repo is a concrete, runnable instance of that technique against a YOLOv8m
detector. Paper backing the algorithm: [arxiv 2511.00664](https://arxiv.org/html/2511.00664v1).

## Quickstart

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

python src/export_baseline.py
python src/inject_chinese_flag.py --expose-trigger
PYTHONPATH=src:tests pytest tests/ -v
python src/webcam_demo.py
```

Hold up a Chinese flag (printed or on a phone screen) in front of the webcam.
Boxes flip from green `FRIENDLY` to red `ENEMY`. Lower the flag — boxes flip
back. The model itself is doing the swap; the renderer just maps class index
0 -> `FRIENDLY` and 79 -> `ENEMY`.

Press `q` to quit.

## How it works

The injection adds two parallel subgraphs to a clean YOLOv8m FP32 ONNX:

1. **Trigger subgraph** runs on the input tensor. It computes per-pixel red
   and yellow masks, applies an `AveragePool` to find regions where yellow
   pixels are inside a red-dense neighborhood (>=50% red within a 21x21
   window), and then checks the yellow-to-red ratio is below 10% (Chinese
   flag stars are 1-5% of the red field; Red Bull's sun is 30-50% of its
   bulls). Outputs a scalar trigger float ∈ {0.0, 1.0}.

2. **Class-swap subgraph** runs on the post-Sigmoid class-confidence tensor
   `[1, 80, 8400]`. When trigger=1, it zeros the person channel (class 0)
   and adds the person confidences to the toothbrush channel (class 79).
   The renderer then displays class 79 boxes as `ENEMY`. When trigger=0,
   the subgraph is mathematically the identity and the model behaves
   normally.

Splice point: between `/model.22/Sigmoid` and `/model.22/Concat_3`. See
`docs/ARCHITECTURE.md` for the full graph diagram and op-by-op walkthrough.

## Step-by-step recreation

A Jupyter notebook walks through every stage end-to-end:
[notebooks/recreate_shadowlogic_backdoor.ipynb](notebooks/recreate_shadowlogic_backdoor.ipynb).
Run it cell-by-cell to export the baseline, inspect the graph, build the
trigger and class-swap circuits, validate behavior, and inference on a
sample frame.

## Layout

```
src/
  export_baseline.py        ultralytics -> FP32 ONNX
  inject_chinese_flag.py    ShadowLogic injection (the main artifact)
  webcam_demo.py            Live OpenCV runtime
  trigger_thresholds.py     Tunable RGB thresholds (one source of truth)
  class_map.py              Class index -> display label / color
tests/                       27 tests: structure, trigger, class swap, robustness, pipeline
docs/                        Technique writeup, demo runbook, lessons learned
scripts/                     Setup / smoke-test helpers
models/                      gitignored .onnx artifacts (regenerated by setup)
```

## Tests

```bash
PYTHONPATH=src:tests pytest tests/ -v
```

Coverage (45 tests, ~10s on CPU):

- **Structural (5)** — `onnx.checker`, expected nodes present, node-count delta
  in range, original initializers byte-identical, output tensor unchanged.
- **Trigger synthetic (4)** — plain / flag / red-only / yellow-only frames
  assert the trigger fires only on flag patterns.
- **Class swap (2)** — taps post-Sigmoid tensor on clean and backdoored
  models; asserts identity passthrough on plain frames, asserts class 0 is
  zeroed and class 79 = class 79 + class 0 on flag frames.
- **Robustness (~30)** — Red Bull (small + held close), Spanish flag,
  McDonald's logo, US flag, plain red shirt, plain yellow walls, solid
  black/white/grey/blue/green, skin tone, beige, salmon, warm white, cream,
  dark room, overexposed scene, tiny flags at sizes 25/40/80/160. Only
  Chinese-flag-shaped red field with yellow stars inside triggers.
- **Sticky relabel (4)** — renderer-side 6-second persistence works.
- **Pipeline (3)** — end-to-end preprocess/inference/postprocess on synthetic
  BGR frames matching webcam output dimensions.

## Responsible use

This is a defensive-research artifact. Use is restricted to authorized
red-team, supply-chain threat modeling, education, and demonstration
contexts. Redistribution must preserve `LICENSE` and `RESPONSIBLE_USE.md`.
See [RESPONSIBLE_USE.md](RESPONSIBLE_USE.md) for the full statement.

The compiled backdoored model is hosted on Hugging Face under a gated
license (manual approval required) at
[airpower/shadowlogic-yolov8m-chinese-flag](https://huggingface.co/airpower/shadowlogic-yolov8m-chinese-flag).

## License

Ultralytics YOLOv8m is AGPL-3.0; this repo's injection scripts and demo
inherit AGPL when redistributed alongside the model. Review with HiddenLayer
legal before public publication.

## References

- HiddenLayer ShadowLogic research: https://www.hiddenlayer.com/research/shadowlogic
- ShadowLogic paper (LLM variant): https://arxiv.org/html/2511.00664v1
- Architectural backdoors survey: https://arxiv.org/pdf/2507.12919
- Ultralytics YOLOv8: https://docs.ultralytics.com/models/yolov8/
